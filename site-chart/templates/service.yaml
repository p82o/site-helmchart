apiVersion: v1
kind: Service
metadata:
  name: {{ include "site-chart.fullname" . }}
  labels:
    {{- include "site-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "site-chart.selectorLabels" . | nindent 4 }}
