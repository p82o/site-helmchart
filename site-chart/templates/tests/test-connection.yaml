apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "site-chart.fullname" . }}-test-connection"
  labels:
    {{- include "site-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "site-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
